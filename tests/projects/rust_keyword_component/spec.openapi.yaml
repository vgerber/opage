openapi: 3.1.0
info:
  title: Test API
  version: 0.0.0
components:
  schemas:
    Box:
      type: object
      description: Test Box
    Geometry:
      type: object
      properties:
        box:
          $ref: '#/components/schemas/Box'